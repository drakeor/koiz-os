#ifndef _VMEM_TESTS_H_
#define _VMEM_TESTS_H_

/**
 * vmem_run_tests() - Runs cloud tests to make sure we can lookup
 *                    vaddrs properly and access paged memory
 */
void vmem_run_tests(void);

#endif